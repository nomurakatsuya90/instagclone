<!DOCTYPE html>
<html>
  <head>
    <title>Fbclone</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="header clearfix">
      <% flash.each do |key, value| %>
        <%= content_tag(:div, value, class: "#{key}") %>
      <% end %>
      <% if logged_in? %>
        <%= link_to "Profile", user_path(current_user.id), class: "left" %>
        <%= link_to "Logout", session_path(current_user.id), method: :delete %>
      <% else %>
        <%= link_to "Sign up", new_user_path %>
        <%= link_to "Login", new_session_path %>
      <% end %>
    </header>
    <section class="main-header">
      <%= image_tag 'facebook_clone_title.png' %>
      <%# <img src="/app/assets/images/img2.jpg" alt="diving画像"> %><!--これだと表示されない-->
    </section>
    <section class="view_part"> 
      <%= yield %>
    </section>
    <%# <footer>
      <div>
        <p>運営会社DIVE INTO CODE株式会社</p>
        <p>© 2015-2018 Copyright DIVE INTO CODE Corp.</p>
      </div>
    </footer> %>
  </body>
</html>
